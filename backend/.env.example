# ─── Server ───────────────────────────────────────────────────
NODE_ENV
PORT

# ─── Database ─────────────────────────────────────────────────
DATABASE_URL

# ─── JWT ──────────────────────────────────────────────────────
JWT_ACCESS_SECRET
JWT_REFRESH_SECRET
JWT_ACCESS_EXPIRES_IN
JWT_REFRESH_EXPIRES_IN

# ─── Email (SendGrid) ─────────────────────────────────────────
SENDGRID_API_KEY
EMAIL_FROM

# ─── App ──────────────────────────────────────────────────────
FRONTEND_URL
APP_NAME

# ─── Bcrypt ───────────────────────────────────────────────────
BCRYPT_SALT_ROUNDS

# ─── OpenAI (Quran Guidance Chatbot) ──────────────────────────
OPENAI_API_KEY
CHATBOT_CONFIDENCE_THRESHOLD=0.68

# ─── AI Memorization-Checker microservice ─────────────────────
# Internal only — must never be reachable from the public internet/frontend.
AI_SERVICE_URL=http://127.0.0.1:8001