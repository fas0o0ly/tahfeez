# Read-only Postgres role — this service must only SELECT from verses/surahs.
DATABASE_URL=postgresql://tahfeez_readonly:password@localhost:5432/tahfeez

# Bind to localhost only — never expose this service publicly.
# The Node backend is the only client.
AI_SERVICE_HOST=127.0.0.1
AI_SERVICE_PORT=8001

WHISPER_MODEL=large-v3
